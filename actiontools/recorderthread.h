#ifndef RECORDERTHREAD_H
#define RECORDERTHREAD_H

#include <QThread>
#include <QKeySequence>

class QLibrary;

/*
namespace ActionTools
{
	class RecorderThread : public QThread
	{
		Q_OBJECT

	public:
		explicit RecorderThread(QObject *parent = 0);
		~RecorderThread();

	protected:
		void run();

	signals:
		void keyPressed(QKeySequence keysequence);

	public slots:

	private:
		Q_DISABLE_COPY(RecorderThread)
	};
}
*/

#endif // RECORDERTHREAD_H
